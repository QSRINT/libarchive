# ===================================================================================
#  The LibArchive CMake configuration file
#
#             ** File generated automatically, do not modify **
#
#  Usage from an external project:
#    In your CMakeLists.txt, add these lines:
#
#    find_package(LibArchive REQUIRED)
#    include_directories(${LibArchive_INCLUDE_DIRS})
#    target_link_libraries(MY_TARGET_NAME ${LibArchive_LIBRARIES})
#
#    This file will define the following variables:
#      - LibArchive_LIBRARIES             : The list of all imported targets for OpenCV modules.
#      - LibArchive_INCLUDE_DIRS          : The LibArchive include directories.
#      - LibArchive_VERSION               : The version of this LibArchive build: "@VERSION_PLAIN@"
#
# ===================================================================================

include(${CMAKE_CURRENT_LIST_DIR}/LibArchiveTargets.cmake)

# ======================================================
#  Version variables:
# ======================================================

SET(LibArchive_VERSION           @LIBARCHIVE_VERSION_STRING@)

# ======================================================
# Include directories to add to the user project:
# ======================================================

# Provide the include directories to the caller
set(LibArchive_INCLUDE_DIRS      "@INCLUDE_DIR@")

# ====================================================================
# Link libraries:
# ====================================================================

set(LibArchive_LIBRARIES         archive_static)
